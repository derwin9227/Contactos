<div class="container my-5">
    <a href="{{route('index')}}" class="btn btn-primary">Inicio</a>
    <a href="{{route('crear')}}" class="btn btn-primary">Agregar</a>
    <a href="{{route('contacto.exportar')}}" class="btn btn-primary">Exportar</a>
</div>